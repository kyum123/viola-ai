# 비올라(Viola) 파이프라인 의존성
# pip install -r requirements.txt

# API 통신 & 환경 변수
requests>=2.31.0
python-dotenv>=1.0.0

# 데이터 처리
pandas>=2.2.2
numpy>=1.26.4

# 공간 데이터 (침수흔적도, 격자 결합)
geopandas>=0.14.4
shapely>=2.0.4

# AI 모델 (규칙 기반 → scikit-learn 고도화)
scikit-learn>=1.4.2

# PostgreSQL + PostGIS 연결
psycopg2-binary>=2.9.9
